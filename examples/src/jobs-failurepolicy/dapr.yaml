version: 1
common:
  daprdLogDestination: console
apps:
  - appID: jobs-example
    appDirPath: ./
    appProtocol: grpc
    appPort: 50051
    logLevel: debug
    schedulerHostAddress: localhost
    command: [ "cargo", "run", "--example", "jobs" ]
